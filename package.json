{
  "name": "@openluckteam/subgraph",
  "version": "0.1.0",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",

    "prepare:bsc": "mustache config/mainnet-bsc.json subgraph.template.yaml > subgraph.yaml && yarn codegen",
    "deploy:bsc": "dotenv -- cross-var graph auth --product hosted-service %THEGRAPH_TOKEN_BSC% && graph deploy --product hosted-service openluckteam/Openluck",

    "prepare:chapel": "mustache config/testnet-chapel.json subgraph.template.yaml > subgraph.yaml && yarn codegen",    
    "deploy:chapel": "dotenv -- cross-var graph auth --product hosted-service %THEGRAPH_TOKEN_BSC% && graph deploy --product hosted-service openluckteam/Openluckdev",
    
    "prepare:testnet-bsc": "mustache config/testnet-bsc.json subgraph.template.yaml > subgraph.yaml && yarn codegen",    
    "create:testnet-bsc": "dotenv -- cross-var graph create openluck-testnet-bsc --node %TESTNET_NODE_BSC%",
    "deploy:testnet-bsc": "dotenv -- cross-var graph deploy openluck-testnet-bsc-v1 --ipfs %TESTNET_IPFS% --node %TESTNET_NODE_BSC%",

    "prepare:node-bsc": "mustache config/mainnet-bsc.json subgraph.template.yaml > subgraph.yaml && yarn codegen",    
    "create:node-bsc": "dotenv -- cross-var graph create openluck-bsc --node %GRAPH_NODE_BSC%",
    "deploy:node-bsc": "dotenv -- cross-var graph deploy openluck-bsc --ipfs %GRAPH_IPFS% --node %GRAPH_NODE_BSC%"

  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.22.1",
    "@graphprotocol/graph-ts": "^0.22.1",
    "mustache": "^4.0.1"
  },
  "devDependencies": {
    "fs": "^0.0.1-security",
    "cross-var": "1.1.0",
    "dotenv-cli": "3.0.0"
  }
}
